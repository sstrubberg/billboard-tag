{
  "id": "billboardtag",
  "author": {
    "name": "Scott Strubberg",
    "email": "sstrubberg@gmail.com"
  },
  "actions": [
    {
      "id": "billboardtag.reviewapply",
      "name": "1. Review and Apply Billboard Tags",
      "description": "Reviews fuzzy-match rows one at a time (approve/skip) and merges exact-match rows automatically, in one pass. Never removes a tag. Only handles small batches - see README.",
      "config": {
        "permissions": {
          "track": {
            "read": ["all"],
            "modify": ["all"],
            "modifyFields": ["tags"]
          },
          "files": {
            "read": true,
            "list": true,
            "write": true
          }
        }
      }
    },
    {
      "id": "billboardtag.changelog",
      "name": "2. View Billboard Changelog",
      "description": "Shows changelog.log — every tag a run has written, most recent first. Read-only.",
      "config": {
        "permissions": {
          "files": {
            "read": true,
            "list": true
          }
        }
      }
    }
  ]
}
