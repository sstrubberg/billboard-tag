{
  "id": "billboardtag",
  "author": {
    "name": "Scott Strubberg",
    "email": "sstrubberg@gmail.com"
  },
  "actions": [
    {
      "id": "billboardtag.reviewapply",
      "name": "Review and Apply Billboard Tags",
      "description": "Reviews fuzzy-match rows one at a time (approve/skip) and merges exact-match rows automatically, in one pass. Never removes a tag. Only handles small batches - see README. After it finishes, run `python view_changelog.py` to see what changed.",
      "config": {
        "permissions": {
          "track": {
            "read": ["all"],
            "modify": ["all"],
            "modifyFields": ["tags"]
          },
          "files": {
            "read": true,
            "list": true,
            "write": true
          }
        }
      }
    }
  ]
}
